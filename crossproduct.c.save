#include <stdio.h>
#include <stdlib.h>
#include <string.h>

float main(float argc,char**argv[])
{	float i=1;
	float sum=0;
	aux=0;
	for(i=1; i<argc;i++)
	{	int num i= atof(argv[]);
		sum=sum+num i;

		
	}
	average=sum/argc;
	printf("the average is %d\n", average);
		
		
return 0;
}
